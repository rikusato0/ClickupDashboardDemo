# =============================================================================
# Production — metrics.whitelotusbk.ai (adjust if your domain changes)
# =============================================================================
# Frontend (SPA):  https://metrics.whitelotusbk.ai
# Backend (HTTP):  https://metrics.whitelotusbk.ai/api
#
# Same-origin: leave VITE_API_BASE_URL unset — apiUrl() uses /api/…
# Split hosts:    VITE_API_BASE_URL=https://api.example.com  (no trailing slash)
# =============================================================================

VITE_APP_ORIGIN=https://metrics.whitelotusbk.ai
